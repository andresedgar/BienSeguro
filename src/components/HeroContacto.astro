---
// Hero section para página de Contacto
---

<section class="hero-contacto">
  <div class="hero-content">
    <div class="text-section">
      <h1 class="hero-title">
        Estamos para ayudarte<br>
        a proteger <span class="highlight">lo que<br>
        más te importa</span>
      </h1>
      <p class="hero-cta">¡CONTÁCTANOS!</p>
    </div>
  </div>
</section>

<style>
  .hero-contacto {
    width: 100%;
    background-image: url('/images/contacto/PORTADA-CONTACTO-2.webp');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    position: relative;
    overflow: hidden;
  }

  .hero-contacto::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.3);
    z-index: 1;
  }

  .hero-content {
    max-width: 1400px;
    margin: 0 auto;
    min-height: 450px;
    position: relative;
    z-index: 2;
    display: flex;
    align-items: center;
  }

  .text-section {
    padding: 2rem;
  }

  .hero-title {
    font-size: 3rem;
    font-weight: 400;
    color: white;
    margin: 0 0 var(--spacing-lg) 0;
    line-height: 1.2;
  }

  .hero-title .highlight {
    color: var(--color-primary);
    font-weight: 700;
  }

  .hero-cta {
    font-size: 2.5rem;
    font-weight: 700;
    color: white;
    margin: 0;
  }

  @media (max-width: 1024px) {
    .hero-content {
      min-height: auto;
    }

    .text-section {
      padding: var(--spacing-2xl) var(--spacing-lg);
      text-align: center;
    }

    .hero-title {
      font-size: 2.5rem;
    }

    .hero-cta {
      font-size: 2rem;
    }
  }

  @media (max-width: 768px) {
    .hero-title {
      font-size: 2rem;
    }

    .hero-cta {
      font-size: 1.75rem;
    }
  }

  @media (max-width: 480px) {
    .text-section {
      padding: var(--spacing-xl) var(--spacing-md);
    }

    .hero-title {
      font-size: 1.75rem;
    }

    .hero-cta {
      font-size: 1.5rem;
    }
  }
</style>
